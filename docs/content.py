"""説明資料の中身。

読む人で2冊に分けてある。
  A 説明する側（導入を説明する人・管理する人）… 何をするシステムで、どう判定し、どう設定するか
  B 操作する側（毎日使う人）                  … 画面の触り方だけ

図は _figures.py にまとめてある（外部ライブラリを使わないSVG）。
"""

from _figures import (
    FLOW_SVG, HOME_SVG, LEDGER_SVG, MIGRATION_SVG, NUMBERING_SVG, PARTIAL_SVG,
    SCREEN_MAP_SVG, SOURCE_SWITCH_SVG, STATUS_FLOW_SVG, STOCK_CURVE_SVG, _ladder_svg,
)


def build_docs(add, cover, badge):
    add("A", "A_システム解説（説明する側）", "システム解説",
        "何をするシステムで、どう判定し、どう設定するか。",
        "#0f766e", doc_guide(cover, badge))
    add("B", "B_操作マニュアル（操作する側）", "操作マニュアル",
        "毎日の画面の触り方。",
        "#1d4ed8", doc_manual(cover, badge))


def toc(items):
    rows = "".join(
        f'<tr><td class="c" style="width:12%"><b>{n}</b></td><td style="width:32%"><b>{t}</b></td>'
        f'<td>{d}</td></tr>'
        for n, t, d in items
    )
    return f'<table style="margin-top:14px">{rows}</table>'


# ══════════════════════════════════════════════════════════════════
# A 説明する側
# ══════════════════════════════════════════════════════════════════

def doc_guide(cover, badge):
    ladder = _ladder_svg(__import__("build").ALERT_COLORS)
    return f"""
{cover("A", "システム解説", "何をするシステムで、どう判定し、どう設定するか。", "説明する側（導入を説明する人・管理する人）向け")}

<h2>目次</h2>

{toc([
  ("1", "何を解決するか", "欠品 / 発注漏れ / 過剰在庫 / 在庫精度の4つ"),
  ("2", "全体の流れ", "入力から対応まで。どこまでが機械で、どこからが人か"),
  ("3", "画面の構成", "9画面の役割と、毎日触る範囲"),
  ("4", "運用サイクル", "毎日・週次・月次に何をするか"),
  ("5", "在庫はこう動く", "発注点・安全在庫・適正在庫上限の意味"),
  ("6", "計算式", "何をどう計算しているか"),
  ("7", "アラート8区分", "判定の順序と優先度"),
  ("8", "設定できること", "マスタ・判定パラメータ・品番の採番"),
  ("9", "データの入れ方", "旧シートからの移行と、毎日の取込"),
  ("10", "運用上の注意", "公開URL・スリープ・バックアップ"),
])}

<h2>1. 何を解決するか</h2>
<p>
  商品ごとの在庫数と直近の出荷実績から、<b>いつ・何を・いくつ発注すべきか</b>を自動で判定します。
</p>
<table>
  <tr><th style="width:20%">課題</th><th>起きていたこと</th><th style="width:34%">このシステムでの見え方</th></tr>
  <tr><td><b>欠品</b></td><td>気づいたときには在庫が無く、納期を落とす</td>
      <td>{badge("欠品")} {badge("欠品リスク")} が優先度Aで出る</td></tr>
  <tr><td><b>発注漏れ</b></td><td>発注したつもりが抜けていた</td>
      <td>{badge("発注漏れ")} が優先度Aで出る（行ごと赤くなる）</td></tr>
  <tr><td><b>過剰在庫</b></td><td>置き場と資金が在庫に寝ている</td>
      <td>{badge("過剰在庫")} {badge("滞留在庫")} が優先度Cで出る</td></tr>
  <tr><td><b>在庫精度</b></td><td>帳簿と現物が合わない</td>
      <td>{badge("棚卸差異")} が優先度Cで出る</td></tr>
</table>
<h2>2. 全体の流れ</h2>
<div class="fig">{FLOW_SVG}
  <div class="caption"><b>図1</b>　入力から対応まで。②〜⑤は毎回その場で計算するので、
  「再計算ボタン」を押す必要がありません。在庫数を直した瞬間に判定が変わります。</div>
</div>

<div class="note">
  <b>スプレッドシート版との一番の違い。</b>
  以前は計算結果をシートに書き込んでいたため、再計算を忘れると前日の判定が残っていました。
  今は保存せず毎回計算するので、古い判定が残ることがありません。
</div>

<h2>3. 画面の構成</h2>
<div class="fig">{SCREEN_MAP_SVG}
  <div class="caption"><b>図2</b>　上部メニューの並びと役割。毎日触るのは左の3つだけです。</div>
</div>
<h2>4. 運用サイクル</h2>
<table>
  <tr><th style="width:14%">頻度</th><th style="width:44%">やること</th><th>使う画面</th></tr>
  <tr><td><b>毎朝</b></td>
      <td>HOMEで優先度A・Bを確認して発注。発注したら対応状況を「発注済み」にする</td>
      <td>HOME → 在庫一覧</td></tr>
  <tr><td><b>その都度</b></td>
      <td>入庫・出庫が発生したら登録する（まとめてCSVでも可）</td><td>入出庫</td></tr>
  <tr><td><b>週1回</b></td>
      <td>優先度Cを見る。過剰在庫・滞留在庫の整理、棚卸差異の原因確認</td>
      <td>在庫一覧（優先度Cで絞る）</td></tr>
  <tr><td><b>月1回</b></td>
      <td>リードタイムや安全在庫日数の実績を見直す。廃番の整理</td>
      <td>商品編集／判定パラメータ</td></tr>
</table>

<div class="pb"></div>
<h2>5. 在庫はこう動く</h2>
<p>在庫は出荷のぶんだけ毎日減り、入荷でまた増えます。このノコギリの形のどこにいるかで、発注すべきかが決まります。</p>
<div class="fig">{STOCK_CURVE_SVG}
  <div class="caption"><b>図3</b>　在庫の推移と3本の線。<b>発注点</b>を割った日に発注すれば、
  リードタイム中に安全在庫を少し食うだけで入荷が間に合います。</div>
</div>
<table>
  <tr><th style="width:20%">線</th><th style="width:42%">意味</th><th>割ったら／超えたら</th></tr>
  <tr><td><b>適正在庫上限</b></td><td>持ちすぎない上限。リードタイム＋安全在庫＋発注サイクル分</td>
      <td>超えると {badge("過剰在庫")}</td></tr>
  <tr><td><b>発注点</b></td><td>リードタイム中に売れる分 ＋ 安全在庫</td>
      <td>割ると {badge("発注推奨")}</td></tr>
  <tr><td><b>安全在庫</b></td><td>納期の遅れや急な注文に備える分</td><td>ここを割ると危険水域</td></tr>
</table>
<h2>6. 計算式</h2>
<pre>有効在庫       = (実棚在庫 があれば実棚 / 無ければ理論在庫) − 引当数
在庫ポジション  = 有効在庫 + 発注残数        ← 発注判定はこの値で行う
平均日次出庫    = 期間出庫数 ÷ 30日
在庫日数       = 有効在庫 ÷ 平均日次出庫
欠品予測日      = 今日 + 在庫日数
安全在庫       = 平均日次出庫 × 安全在庫日数(既定7日)
発注点         = 平均日次出庫 × リードタイム + 安全在庫
適正在庫上限    = 平均日次出庫 × (リードタイム + 安全在庫日数 + 発注サイクル14日)
発注推奨数      = 適正在庫上限まで戻す量を、MOQ と発注ロット単位に切り上げた数
発注期限日      = 欠品予測日 − リードタイム</pre>

<div class="note">
  <b>なぜ「有効在庫＋発注残」で判定するのか。</b>
  手元の在庫だけで見ると、発注済みの商品にも毎日「発注してください」と出続けます。
  そうなるとアラート自体が信用されなくなり、本当に危ない行も見逃されます。
</div>

<h3>商品ごとに変えられる値</h3>
<table>
  <tr><th style="width:28%">項目</th><th>効き方</th></tr>
  <tr><td>発注リードタイム日数</td><td><b>発注点計算の中核。</b>長いほど早めに発注点に当たる</td></tr>
  <tr><td>安全在庫日数</td><td>空欄なら既定7日。切らしたくない商品は長くする</td></tr>
  <tr><td>最小発注数(MOQ)</td><td>発注推奨数の下限</td></tr>
  <tr><td>発注ロット単位</td><td>ケース入数など。発注推奨数はこの倍数に切り上がる</td></tr>
</table>
<div class="pb"></div>
<h2>7. アラート8区分</h2>
<p>8つの条件を<b>重い順</b>に見て、最初に当たった1つだけを表示します。1つの商品に2つ付くことはありません。</p>
<div class="fig">{ladder}
  <div class="caption"><b>図4</b>　判定の順序。当たらなければ次の条件に進みます。</div>
</div>
<table>
  <tr><th style="width:18%" class="c">優先度</th><th style="width:30%">意味</th><th>いつ見るか</th></tr>
  <tr><td class="c"><span class="badge" style="background:#f4cccc;color:#990000">A:即日対応</span></td>
      <td>今日中に手を打たないと欠品する</td><td>毎朝いちばんに</td></tr>
  <tr><td class="c"><span class="badge" style="background:#ffedc2;color:#7f6000">B:3日以内</span></td>
      <td>3日以内に発注すれば間に合う</td><td>毎朝。Aを片付けた後</td></tr>
  <tr><td class="c"><span class="badge" style="background:#d8e6f4;color:#1c4587">C:要確認</span></td>
      <td>すぐ欠品はしないが、在庫が傷んでいる</td><td>週に1回まとめて</td></tr>
</table>
<h2>8. 設定できること</h2>
<h3>判定パラメータ（全商品に共通）</h3>
<table>
  <tr><th style="width:24%">項目</th><th style="width:9%" class="c">既定</th>
      <th style="width:33%">大きくすると</th><th>小さくすると</th></tr>
  <tr><td>消化傾向の集計期間</td><td class="c">30日</td>
      <td>判定が安定するが、需要の変化に鈍くなる</td><td>季節変動に追従するが、数字がブレやすい</td></tr>
  <tr><td>安全在庫日数（既定値）</td><td class="c">7日</td>
      <td>欠品しにくくなるが、在庫が増える</td><td>在庫は減るが、欠品リスクが上がる</td></tr>
  <tr><td>発注サイクル</td><td class="c">14日</td>
      <td>発注回数は減るが、1回の量と在庫が増える</td><td>こまめな発注になり、事務負荷が上がる</td></tr>
  <tr><td>滞留とみなす日数</td><td class="c">90日</td>
      <td>滞留の警告が出にくくなる</td><td>季節商品が滞留扱いされやすい</td></tr>
  <tr><td>棚卸差異を見逃す範囲</td><td class="c">0</td>
      <td>小さなズレを無視できる</td><td>1個のズレも棚卸差異として出る</td></tr>
</table>
<div class="tip">
  <b>いじる順番。</b>まず既定のまま1か月運用し、「欠品が出た商品は安全在庫日数を延ばす」
  「毎週同じ商品で発注推奨が出て事務が重いなら発注サイクルを延ばす」というように、
  実績を見てから1つずつ動かすのが安全です。
</div>

<h3>マスタ</h3>
<table>
  <tr><th style="width:22%">画面</th><th style="width:32%">持っている項目</th><th>入力制限</th></tr>
  <tr><td>マスタ（対応状況）</td><td>名前・色・並び順</td><td><b>あり。</b>ここに無い値は選べない</td></tr>
  <tr><td>マスタ（カテゴリ・単位）</td><td>名前・並び順</td><td>なし。候補として出すだけ</td></tr>
  <tr><td>仕入先</td><td>名前・仕入先コード</td><td>なし。商品編集で選ぶとコードも入る</td></tr>
  <tr><td>保管場所</td><td>名前・並び順</td><td>なし</td></tr>
  <tr><td>出荷先</td><td>名前・コード・区分・住所・担当者・電話</td><td>出庫の登録時に選ぶ</td></tr>
</table>
<div class="warn">
  <b>使われているものは削除できません。</b>
  消すと、その商品や出庫の相手先が宙に浮いた文字列として残るためです。
  一覧に使用件数を出しているので、0件のものだけ削除でき、使用中は「非表示」で運用から外します。
</div>

<h3>品番（商品コード）の採番</h3>
<div class="fig">{NUMBERING_SVG}
  <div class="caption"><b>図5</b>　カテゴリごとに「接頭辞＋連番」を決めておくと、新規登録時に発番できます。</div>
</div>

<div class="pb"></div>
<h2>9. データの入れ方</h2>
<div class="fig">{MIGRATION_SVG}
  <div class="caption"><b>図6</b>　旧スプレッドシートからの移行。書き出してそのまま取り込むだけです。</div>
</div>
<p>
  自動計算列（有効在庫・発注点・アラート区分など）が入っていても読み飛ばすので、シートをそのまま出して構いません。
  必須は<b>「商品コード」と「商品名」</b>の2つだけで、並び順は問いません。
</p>

<h3>毎日のデータは部分更新で入れられる</h3>
<div class="fig">{PARTIAL_SVG}
  <div class="caption"><b>図7</b>　CSVに載っていない列は現在値のまま。だから毎日の販売データを部分更新で流せます。</div>
</div>

<h3>入出庫を入れると判定の材料が変わる</h3>
<div class="fig">{SOURCE_SWITCH_SVG}
  <div class="caption"><b>図8</b>　出庫を1件でも登録した商品は、期間出庫数を台帳から自動集計します。
  商品ごとに切り替わるので、全商品を一度に揃える必要はありません。</div>
</div>
<div class="pb"></div>
<h2>10. 運用上の注意</h2>

<h3>判定は保存していない</h3>
<p>
  有効在庫・発注点・アラート区分などはデータベースに持たず、画面を開くたびに計算しています。
  だから「再計算を忘れて古い判定が残る」ことがなく、数字を直せばその場で判定が変わります。
  裏を返すと、<b>これらの列は直接編集できません</b>。もとになる数字を直してください。
</p>

<h3>アラートは機械、対応は人</h3>
<p>
  対応状況（発注済み・保留・廃盤・発注NG）の列は、計算で書き換わることが絶対にありません。
  「発注漏れ」と出ていても、対応状況が「発注済み」なら手当て済みとして扱います。
</p>

<h3>公開URLとアクセス制限</h3>
<table>
  <tr><th style="width:26%">項目</th><th>内容</th></tr>
  <tr><td>ログイン</td><td>画面内のログインは無い。URLを開いた時点で出る<b>ID・パスワード（Basic認証）</b>で入口を塞いでいる</td></tr>
  <tr><td>スリープ</td><td>無料プランは一定時間アクセスが無いと停止する。平日9〜21時は自動で起こしているが、
      夜間・休日の初回は表示に30秒ほどかかる</td></tr>
  <tr><td>バックアップ</td><td>「取込・書き出し」から42項目のCSVと入出庫CSVを書き出しておく</td></tr>
</table>

<h3>日次アラート</h3>
<p>
  優先度A・Bの一覧を毎朝メールで送れます（設定したときのみ）。
  画面を開かなくても気づけるので、朝いちばんの確認をメールに寄せる運用もできます。
</p>

<div class="tip">
  <b>導入の進め方。</b>
  ①旧シートをCSVで取り込む → ②マスタ（仕入先・保管場所・出荷先）を自社のものに直す →
  ③1〜2週間そのまま運用してアラートの出方を見る → ④多すぎる／少なすぎる区分があれば
  判定パラメータか商品ごとのリードタイム・安全在庫日数を調整、の順が安全です。
</div>
"""


# ══════════════════════════════════════════════════════════════════
# B 操作する側
# ══════════════════════════════════════════════════════════════════

def doc_manual(cover, badge):
    return f"""
{cover("B", "操作マニュアル", "毎日の画面の触り方。")}

<h2>目次</h2>

{toc([
  ("1", "1日の流れ", "朝に見る・その都度入れる・週に1回片付ける"),
  ("2", "HOMEを開く", "今日どれだけ手を打つ必要があるか"),
  ("3", "在庫一覧を使う", "絞り込み・並べ替え・列の出し入れ"),
  ("4", "対応状況を付ける", "発注したら「発注済み」にする"),
  ("5", "入出庫を登録する", "入庫・出庫・調整の入れ方"),
  ("6", "出荷先と入庫元", "どこへ出したか・どこから入ったか"),
  ("7", "商品を追加する", "品番は「採番」ボタンで発番できる"),
  ("8", "選択肢を足す", "仕入先・保管場所・出荷先の新規登録"),
  ("9", "CSVで取り込む", "まとめて入れる・書き出す"),
  ("10", "困ったとき", "よくある質問"),
])}

<h2>1. 1日の流れ</h2>
<table>
  <tr><th style="width:16%">いつ</th><th style="width:40%">すること</th><th>画面</th></tr>
  <tr><td><b>朝</b></td><td>優先度A・Bを確認して発注する。発注したら対応状況を「発注済み」に</td><td>HOME</td></tr>
  <tr><td><b>その都度</b></td><td>入庫・出庫があったら登録する</td><td>入出庫</td></tr>
  <tr><td><b>週1回</b></td><td>優先度Cを見る（過剰在庫・滞留在庫・棚卸差異）</td><td>在庫一覧</td></tr>
</table>
<div class="pb"></div>
<h2>2. HOMEを開く</h2>
<div class="fig">{HOME_SVG}
  <div class="caption"><b>図1</b>　上の件数 → 区分の内訳 → 要対応リスト、の順に見ます。</div>
</div>
<ol class="steps">
  <li><b>A（即日対応）の件数を見る。</b>0件なら、今日中に手を打つべき商品はありません。</li>
  <li><b>要対応リストを上から片付ける。</b>「推奨アクション」に
      「本日中に 230 発注（リードタイム7日）」のように数量と期限が書かれています。</li>
  <li><b>発注したら対応状況を「発注済み」に変える。</b>一覧のプルダウンからその場で変えられます。</li>
</ol>
<h2>3. 在庫一覧を使う</h2>
<p>旧スプレッドシートと同じ42項目が、同じ並びで入っています。</p>

<h3>左3列は固定されている</h3>
<p>横にスクロールしても<b>対応状況・商品コード・商品名</b>は画面に残ります。</p>

<h3>見たい区分だけ表示する</h3>
<p>
  表の上の「表示する区分」を押すと、その区分の列がまとめて隠れます。
  選んだ状態はブラウザが覚えているので、次に開いたときも同じ見え方になります。
</p>
<table>
  <tr><th style="width:24%">区分</th><th>入っている項目</th></tr>
  <tr><td>基本情報</td><td>カテゴリ / 規格・型番 / 単位 / 保管場所</td></tr>
  <tr><td>仕入先・発注条件</td><td>仕入先 / 単価 / リードタイム / MOQ / ロット</td></tr>
  <tr><td>在庫状況</td><td>理論在庫 / 実棚在庫 / 棚卸差異 / 引当 / 発注残 / 有効在庫 / 入荷予定日 / 在庫金額</td></tr>
  <tr><td>消化傾向</td><td>期間出庫数 / 平均日次出庫 / 在庫日数 / 欠品予測日</td></tr>
  <tr><td>発注判定</td><td>安全在庫 / 発注点 / 適正在庫上限 / 発注推奨数 / 発注期限日</td></tr>
  <tr><td>アラート</td><td>アラート区分 / 優先度 / 理由 / 推奨アクション</td></tr>
  <tr><td>履歴・管理</td><td>最終入庫日 / 最終出庫日 / 最終棚卸日 / 滞留日数 / 備考 / 更新日時</td></tr>
</table>

<h3>絞り込みと並べ替え</h3>
<ul>
  <li>キーワード（商品コード・商品名・型番・仕入先）、アラート区分、優先度、対応状況、カテゴリ、保管場所、仕入先で絞れます</li>
  <li>絞り込んだ状態はURLに残るので、そのままブックマークや共有ができます</li>
  <li>見出しをクリックすると並べ替わります</li>
  <li>「CSVで書き出す」は、<b>いま絞り込んでいるものだけ</b>を出します（仕入先ごとの発注リストに便利）</li>
</ul>

<div class="note">
  <b>「自動」と付いた列は直せません。</b>
  有効在庫・発注点・アラート区分などは、開くたびに計算し直しているためです。
  変えたいときは、もとになる数字（理論在庫・引当・リードタイムなど）を直します。
</div>
<div class="pb"></div>
<h2>4. 対応状況を付ける</h2>
<div class="fig">{STATUS_FLOW_SVG}
  <div class="caption"><b>図2</b>　対応状況の流れ。人が選ぶ列で、計算で書き換わることはありません。</div>
</div>
<table>
  <tr><th style="width:20%">対応状況</th><th>使うとき</th></tr>
  <tr><td><b>即日対応</b></td><td>今日中に発注・手配すると決めた行に付けて、作業待ちを見えるようにする</td></tr>
  <tr><td><b>要確認</b></td><td>数字が怪しい・現物を見てから決める行</td></tr>
  <tr><td><b>発注済み</b></td><td>発注を出した行。HOMEの「手当て済み」件数に入る</td></tr>
  <tr><td><b>保留</b></td><td>今は動かさないと決めた行</td></tr>
  <tr><td><b>廃盤</b></td><td>もう仕入れない商品。在庫を売り切る対象</td></tr>
  <tr><td><b>発注NG</b></td><td>仕入先都合などで発注できない行</td></tr>
</table>
<div class="tip">
  <b>アラートが消えるのはいつか。</b>
  対応状況を変えてもアラート区分は消えません（機械の判定と人の対応は別物なので）。
  発注残（入荷予定の数）を入力するか、入庫を登録した時点で判定そのものが変わります。
</div>

<h2>5. 入出庫を登録する</h2>
<div class="fig">{LEDGER_SVG}
  <div class="caption"><b>図3</b>　登録するたびに理論在庫が動きます。取り消せば同じだけ戻ります。</div>
</div>
<table>
  <tr><th style="width:14%" class="c">区分</th><th style="width:20%">在庫への効き方</th><th>使うとき</th></tr>
  <tr><td class="c"><span class="badge" style="background:#d1fae5;color:#065f46">入庫</span></td>
      <td>増える（＋）</td><td>仕入先から入荷した／他倉庫から移ってきた／現場から返ってきた</td></tr>
  <tr><td class="c"><span class="badge" style="background:#e0f2fe;color:#075985">出庫</span></td>
      <td>減る（−）</td><td>得意先・現場へ出荷した／自社で使った</td></tr>
  <tr><td class="c"><span class="badge" style="background:#fef3c7;color:#92400e">調整</span></td>
      <td>符号のとおり（±）</td><td>棚卸で差異が出た／破損・紛失を反映する</td></tr>
</table>

<div class="pb"></div>
<h3>登録の手順</h3>
<ol class="steps">
  <li><b>「入出庫」を開く。</b>（商品の明細画面からでも同じフォームが使えます）</li>
  <li><b>商品を探す。</b>品番でも商品名でも型番でも、一部を打てば候補が出ます。
      候補には<b>商品名と現在庫</b>が並ぶので、品番が合っているかその場で確かめられます。</li>
  <li><b>日付・区分・数量</b>を入れます。日付は今日が最初から入っています。</li>
  <li><b>出庫なら出荷先、入庫なら入庫元</b>を選びます（どちらも任意）。</li>
  <li><b>伝票番号・備考</b>を入れて「登録する」。理論在庫がその場で増減します。</li>
</ol>

<div class="note">
  <b>入庫・出庫にマイナスは入れられません。</b>
  減らしたいときは区分を「調整」にして、マイナスの数量で入れます。
  入庫と出庫の合計が実態と合わなくなり、後から履歴を追えなくなるためです。
</div>

<h2>6. 出荷先と入庫元</h2>
<table>
  <tr><th style="width:16%">項目</th><th style="width:16%">つく区分</th><th>選び方</th></tr>
  <tr><td><b>出荷先</b></td><td>出庫のみ</td>
      <td>「出荷先」画面に登録したものから選ぶ</td></tr>
  <tr><td><b>入庫元</b></td><td>入庫のみ</td>
      <td>仕入先と、これまでに入力した入庫元が候補に出る。候補に無い相手も自由に書ける</td></tr>
</table>
<p>
  「入出庫」画面と「出荷先」画面に、<b>直近30日でどこへいくつ出たか</b>が数量・件数・金額で出ます。
  「明細」を押すと、その出荷先の出庫だけに絞った履歴が開きます。
</p>
<div class="pb"></div>
<h2>7. 商品を追加する</h2>
<ol class="steps">
  <li>「在庫一覧」→ 右上の<b>「商品を追加」</b></li>
  <li><b>カテゴリを選んでから「採番」を押す</b>と、品番が自動で入ります（手入力も可）</li>
  <li>商品名・単位・保管場所・仕入先を入れる。仕入先名を選ぶと<b>仕入先コードも一緒に入ります</b></li>
  <li><b>発注リードタイム日数を必ず入れる。</b>ここが空欄だと発注判定が働きません</li>
  <li>理論在庫数・引当数・期間出庫数などを入れて「保存する」</li>
</ol>
<div class="warn">
  <b>商品コードは後から変えないでください。</b>
  CSV取込の照合キーであり、入出庫の履歴もこのコードで結び付いています。
</div>

<h2>8. 選択肢を足す</h2>
<p>
  仕入先・保管場所・出荷先は、それぞれ専用ページの<b>右上「＋ 新規登録」</b>から足します。
  行ごとの「編集」を押したときだけ入力欄に変わります。
</p>
<ul>
  <li>対応状況・カテゴリ・単位は「マスタ」画面にあります</li>
  <li><b>使われているものは削除できません。</b>一覧の使用件数が0のものだけ消せます。
      使わなくなったものは「非表示」に切り替えてください（一覧には残りますが、選択肢に出なくなります）</li>
</ul>

<h2>9. CSVで取り込む</h2>
<ol class="steps">
  <li>「取込・書き出し」を開く</li>
  <li>商品なら<b>商品CSV</b>、入出庫なら<b>入出庫CSV</b>にファイルを指定（シートからの貼り付けも可）</li>
  <li><b>「内容を確認する」</b>で件数とエラーを見る</li>
  <li>問題なければ<b>「◯件を取り込む」</b></li>
</ol>
<table>
  <tr><th style="width:26%">種類</th><th>必須の見出し</th><th style="width:30%">エラーがあったら</th></tr>
  <tr><td>商品CSV</td><td>商品コード / 商品名</td><td>その行だけ飛ばして取り込む</td></tr>
  <tr><td>入出庫CSV</td><td>日付 / 区分 / 商品コード / 数量</td><td><b>1件も取り込まない</b>（在庫が合わなくなるため）</td></tr>
</table>
<div class="pb"></div>
<h2>10. 困ったとき</h2>

<h3>発注したのにアラートが消えない</h3>
<p>
  対応状況を「発注済み」にしても、アラート区分は消えません（機械の判定と人の対応は別だからです）。
  商品編集で<b>発注残数（入荷予定）</b>を入れるか、入庫を登録すると判定そのものが変わります。
</p>

<h3>在庫数を直したい</h3>
<p>
  実地棚卸の結果なら<b>実棚在庫数</b>に入れてください（入っていればそちらが現物として扱われます）。
  入出庫を使っている商品なら、区分「調整」で差分を入れるほうが履歴に残って追いやすくなります。
</p>

<h3>入出庫を間違えて登録した</h3>
<p>履歴の行の右にある<b>「取消」</b>を押してください。在庫も同じだけ戻ります。</p>

<h3>「発注推奨数」が思ったより多い／少ない</h3>
<p>
  発注推奨数は「適正在庫上限まで戻す量」を、最小発注数(MOQ)と発注ロット単位に切り上げた数です。
  多すぎるならロット単位かMOQを、少なすぎるなら安全在庫日数やリードタイムを確認してください。
</p>

<h3>期間出庫数が自分で入れた値と違う</h3>
<p>
  その商品で出庫を登録していると、<b>直近30日の出庫の合計</b>が自動で使われます（一覧に「台帳」と印が付きます）。
  手入力の値に戻したい場合は、その商品の出庫の登録を取り消してください。
</p>

<h3>画面が出てくるまで遅い</h3>
<p>
  夜間・休日の最初の1回は、システムが休止状態から起きるため30秒ほどかかります。
  平日の9〜21時は起こしてあるので、待たされることはありません。
</p>

<h3>ID・パスワードを聞かれる</h3>
<p>URLを開いた時点で出る入口の鍵です。管理者に確認してください。</p>
"""
