# 在庫・発注アラート 設計書

サイト版（Next.js + Supabase）の設計。
スプレッドシート版からの変更点は「どこで計算するか」だけで、計算式とアラート区分は同じ。

## 0. 全体の流れ

```
CSV取込 / 画面入力 / 入出庫の登録
   ↓ inventory_items（入力列だけを保存する）＋ inventory_movements（入出庫1件ずつ）
現在庫（理論・実棚 → 有効在庫）
   ↓ 消化傾向（直近30日の出庫 → 平均日次出庫 → 在庫日数）
閾値（安全在庫・発注点・適正在庫上限）
   ↓ 判定  ← inventory_items_evaluated ビューが読むたびに計算
発注候補（推奨数・期限）＋ アラート（8区分／優先度A・B・C）
   ↓
画面（ダッシュボード・在庫一覧）／ 日次アラート（/api/alerts/daily）
```

**判定結果は保存しない。** 開くたびに、その日の日付で計算し直す。
スプレッドシート版は `recalcAll` が自動列を書き込んでいたため、再計算を忘れると
前日の判定が残り続けた。ビューにしたことで「在庫数を直した瞬間に判定が変わる」ようになり、
再計算ボタンも日次バッチも要らなくなった。

## 1. 項目定義（42項目）

区分 | # | 項目 | 保存/計算 | 内容
:--|--:|:--|:--:|:--
対応状況 | 1 | 対応状況 | 保存 | **人が決める列**。マスタの選択肢から選ぶ
基本情報 | 2 | 商品コード | 保存 | SKU。全処理のキー。重複禁止（DBのunique制約）
 | 3 | 商品名 | 保存 |
 | 4 | カテゴリ | 保存 | 分類別の集計・絞り込みに使う
 | 5 | 規格・型番 | 保存 | サイズ／色／型番
 | 6 | 単位 | 保存 | 個・箱・本 など
 | 7 | 保管場所 | 保存 | 倉庫・棚番
仕入先・発注条件 | 8 | 仕入先コード | 保存 | マスタで仕入先名を選ぶと自動で入る
 | 9 | 仕入先名 | 保存 | 発注書の宛先
 | 10 | 仕入単価 | 保存 | 在庫金額の計算に使用
 | 11 | 販売単価 | 保存 | 欠品時の機会損失額の試算用
 | 12 | 発注リードタイム日数 | 保存 | **発注点計算の中核**。発注〜入荷までの日数
 | 13 | 最小発注数(MOQ) | 保存 | 発注推奨数の下限
 | 14 | 発注ロット単位 | 保存 | ケース入数など。この倍数に切り上げる
在庫状況 | 15 | 理論在庫数 | 保存 | 帳簿上の在庫
 | 16 | 実棚在庫数 | 保存 | 棚卸実数。入っていればこちらを正とする（空欄＝未棚卸）
 | 17 | 棚卸差異 | 計算 | 実棚 − 理論
 | 18 | 引当数(受注残) | 保存 | 受注済みで出荷待ちの数
 | 19 | 発注残数(入荷予定) | 保存 | 発注済みで未入荷の数
 | 20 | 有効在庫数 | 計算 | 手持ち − 引当。**実際に売れる在庫**
 | 21 | 入荷予定日 | 保存 | 遅延判定に使用
 | 22 | 在庫金額 | 計算 | 有効在庫 × 仕入単価
消化傾向 | 23 | 期間出庫数(直近30日) | 保存 | 販売データから取込。期間は判定パラメータで変えられる
 | 24 | 平均日次出庫数 | 計算 | 期間出庫数 ÷ 集計期間
 | 25 | 在庫日数 | 計算 | 有効在庫 ÷ 平均日次出庫。**あと何日もつか**
 | 26 | 欠品予測日 | 計算 | 今日 ＋ 在庫日数
発注判定 | 27 | 安全在庫日数 | 保存 | 空欄なら既定7日。重要商品は長く設定
 | 28 | 安全在庫数 | 計算 | 平均日次出庫 × 安全在庫日数
 | 29 | 発注点 | 計算 | 平均日次出庫 × リードタイム ＋ 安全在庫
 | 30 | 適正在庫上限 | 計算 | 平均日次出庫 ×（リードタイム＋安全在庫日数＋発注サイクル14日）
 | 31 | 発注推奨数 | 計算 | 上限まで戻す量をMOQ・ロットに丸めた数
 | 32 | 発注期限日 | 計算 | 欠品予測日 − リードタイム。**この日までに発注**
アラート | 33 | アラート区分 | 計算 | 下記8区分
 | 34 | 優先度 | 計算 | A:即日対応 / B:3日以内 / C:要確認
 | 35 | アラート理由 | 計算 | 判定根拠の数値（文面はアプリ側 `alert-text.ts`）
 | 36 | 推奨アクション | 計算 | 次に取るべき行動（同上）
履歴・管理 | 37 | 最終入庫日 | 保存 |
 | 38 | 最終出庫日 | 保存 | 滞留判定に使用
 | 39 | 最終棚卸日 | 保存 | 棚卸の鮮度管理
 | 40 | 滞留日数 | 計算 | 今日 − 最終出庫日
 | 41 | 備考 | 保存 | 廃番予定・季節商品など
 | 42 | 更新日時 | 保存 | 行を更新したときにDBのトリガーが入れる

## 1-2. 保存する列としない列

区分 | 実体 | 誰が埋めるか
:--|:--|:--
保存する列（25項目） | `inventory_items` のカラム | 人（画面）／CSV取込
計算する列（17項目） | `inventory_items_evaluated` ビュー | 読むたびにDBが計算する

計算列はテーブルに存在しないので、**上書き事故が起きない**。
スプレッドシート版では「再計算が手入力を値で潰す」ことを `PROTECTED_COLS` で防いでいたが、
その仕組み自体が不要になった。

対応状況（1列目）だけは例外的に、一覧から直接変更できるようにしてある。
**アラートは機械が出し、対応は人が決める。** アラート区分が「発注漏れ」のままでも、
対応状況が「発注済み」なら対応済みとみなす — この2つの並びが運用の軸になる。

### 判定ロジックの置き場所

- **区分の判定と数値の計算** … `supabase/migrations/20260924150010_inventory_evaluated_view.sql`
- **理由・推奨アクションの文面** … `webapp/src/lib/inventory/alert-text.ts`

判定そのものはDBに1箇所だけ置く（一覧の絞り込み・並べ替え・書き出し・日次アラートが
同じ判定を共有するため）。文章の組み立てだけをアプリ側に置いている。

## 2. 計算ロジック

```
有効在庫       = (実棚在庫 があれば実棚 / 無ければ理論在庫) − 引当数
在庫ポジション  = 有効在庫 + 発注残数        ← 発注判定はこの値で行う
平均日次出庫    = 期間出庫数 ÷ 30
在庫日数       = 有効在庫 ÷ 平均日次出庫
安全在庫       = 平均日次出庫 × 安全在庫日数
発注点         = 平均日次出庫 × リードタイム + 安全在庫
適正在庫上限    = 平均日次出庫 × (リードタイム + 安全在庫日数 + 発注サイクル)
発注推奨数      = ceil( max(適正在庫上限 − 在庫ポジション, MOQ) ÷ ロット ) × ロット
発注期限日      = 欠品予測日 − リードタイム
```

**要点**：発注判定を「有効在庫」ではなく「有効在庫＋発注残」で行うこと。
これをしないと、発注済みの商品に対して毎日重複発注のアラートが出続ける。

「今日」は `now() at time zone 'Asia/Tokyo'` で出す。UTCの日付で判定すると、
朝9時前は前日扱いになり発注期限が1日ずれる。

## 3. アラート区分（上から優先。最初に該当したものを採用）

# | 区分 | 条件 | 優先度 | 解決する課題
--:|:--|:--|:--:|:--
1 | 欠品 | 有効在庫 ≦ 0 | A | 欠品
2 | 発注漏れ | 発注点割れ かつ 発注残0 かつ 発注期限日を超過 | A | 発注漏れ
3 | 欠品リスク | 在庫日数 ≦ リードタイム | A | 欠品
4 | 入荷遅延 | 入荷予定日 < 今日 かつ 発注残 > 0 | B | 欠品
5 | 発注推奨 | 在庫ポジション ≦ 発注点 | B | 発注漏れ
6 | 過剰在庫 | 有効在庫 > 適正在庫上限 | C | 過剰在庫
7 | 滞留在庫 | 90日以上 出庫なし かつ 在庫あり | C | 過剰在庫
8 | 棚卸差異 | 実棚 ≠ 理論 | C | 在庫精度

優先度の意味：**A=今日中に手を打たないと欠品する / B=3日以内に発注 / C=週次で棚卸し**

優先度はこの番号から決まる（1〜3=A / 4〜5=B / 6〜8=C）。
並べ替えには `priority_rank`・`alert_rank` を使う。文字列の優先度をそのまま昇順にすると
`-`（対象外）が `A` より先に来てしまうため。

## 4. 運用サイクル

頻度 | やること | 画面・入口
:--|:--|:--
毎日 | 販売データ（商品コード＋期間出庫数）を取り込む | 取込・書き出し
毎日（自動） | 優先度A/Bをメール通知 | `GET /api/alerts/daily?token=…&send=1` を cron から
随時 | 要対応リストを見て発注し、対応状況を「発注済み」にする | ダッシュボード／在庫一覧
週次 | Cアラート（過剰・滞留・棚卸差異）の棚卸し | 在庫一覧で優先度Cに絞る
月次 | リードタイム／安全在庫日数の実績見直し、廃番整理 | 商品編集／判定パラメータ

判定そのものは常に最新なので、「再計算を回す」作業は無い。

## 5. 判定パラメータ（画面から変更できる。旧 `CONFIG`）

保存先は `inventory_settings` の1行。変えると全商品の判定がその場で変わる。

- `demand_window_days: 30` — 消化傾向の集計期間。季節品は14日に短縮すると追従が速い
- `default_safety_days: 7` — 安全在庫日数の既定値（商品ごとに上書き可）
- `order_cycle_days: 14` — 1回の発注で何日分補充するか。発注回数と在庫量のトレードオフ
- `dead_stock_days: 90` — 滞留在庫とみなす日数
- `count_diff_tolerance: 0` — 棚卸差異を許容する絶対値
- `no_move_days: 9999` — 出庫実績ゼロ品の在庫日数の代用値（画面には出さない）

## 6. マスタ（旧「マスタ」シート）

保存先は `inventory_master_options`。`kind` で5種類を1テーブルに束ねている。

kind | 対象項目 | 入力制限 | 画面
:--|:--|:--|:--
status | 対応状況 | **あり**（選択肢からのみ選ぶ） | マスタ
category | カテゴリ | なし（候補として出すだけ） | マスタ
unit | 単位 | なし | マスタ
location | 保管場所 | なし | **保管場所**（専用ページ）
supplier | 仕入先名（＋仕入先コード） | なし | **仕入先**（専用ページ）

仕入先・保管場所・出荷先は登録件数が増えやすく一覧で見たいので、専用ページに分けてある。
どれも「一覧＋新規登録ボタン」で、追加・編集のときだけ入力欄を出す
（入力欄を常に並べると、登録済みの一覧が読み取りにくくなるため）。

対応状況だけ制限をかけるのは、人が絞り込みに使う列で表記ゆれが致命的になるため。
残り4つを候補どまりにするのは、CSV取込で新しい値が来たときに取込自体を失敗させないため
（取り込んだうえで「マスタにありません」と警告する）。

商品で使われている選択肢は削除できない。使わなくなったものは「表示しない」に切り替える
（消すと、その商品の値だけがマスタ外の文字列として宙に浮くため）。

## 6-2. 入出庫（inventory_movements）と出荷先（inventory_destinations）

在庫数を手で書き換える代わりに、入出庫を1件ずつ積み上げるための台帳。

列 | 内容
:--|:--
item_id | 対象の商品
moved_on | 計上日
kind | `in`（入庫） / `out`（出庫） / `adjust`（調整）
quantity | 入庫・出庫は正の数。調整だけ符号つき（減らすときはマイナス）
signed_qty | 在庫への効き方。`out` なら負、それ以外は quantity のまま（生成列）
destination_id | 出荷先。**出庫のときだけ**入る（DB制約で担保）
source_name | 入庫元。**入庫のときだけ**入る（同上）
slip_no / note | 伝票番号・備考

### 理論在庫数の持ち方

`inventory_items.book_qty` を**残高**として持ち、入出庫のトリガー
（`inventory_apply_movement`）が差分だけ増減させる。台帳の合計を毎回数え直す方式にしないのは、
過去分を後からCSVで流し込んだときに「今の残数」が過去の合計で上書きされてしまうため。

修正・削除でも同じだけ戻る（UPDATE は古い分を引いてから新しい分を足す）。

### 台帳がある商品・ない商品

判定ビューは商品ごとに台帳を集計し、**出庫が1件でもあれば台帳の値を使う**。

項目 | 台帳がある商品 | 台帳が無い商品
:--|:--|:--
期間出庫数 | 直近 `demand_window_days` 日の出庫合計 | `out_qty_window`（手入力・CSV）
最終入庫日 | 入庫の最新日 | `last_in_date`（手入力・CSV）
最終出庫日 | 出庫の最新日 | `last_out_date`（手入力・CSV）

全商品の入出庫を揃えなくても移行できるようにするための作り。
一覧では、台帳から集計した期間出庫数に「台帳」と印を付けて区別している。

### 出荷先と入庫元

**出荷先**（`inventory_destinations`）は、仕入先（マスタの1種類）と違い
住所・担当者・電話まで持たせたいので独立したテーブルにしてある。
出庫実績が1件でもある出荷先は削除できない（消すと、その出庫の行き先が分からなくなるため）。
入出庫画面では、直近 `demand_window_days` 日の出荷先別の数量・件数・金額（販売単価ベース）を出す。

**入庫元**（`source_name`）はテーブルを作らず文字列で持つ。入庫元のほとんどは既に仕入先マスタに
ある名前で、住所や担当者を別に管理する必要が無いため。商品の「仕入先名」と同じく、
仕入先マスタと過去の入力実績を候補に出しつつ、マスタに無い相手（他倉庫からの移管、
現場からの返却など）も書けるようにしている。

## 6-3. 品番（商品コードの採番ルール）

`inventory_code_rules` に「接頭辞＋区切り＋連番（桁数指定）」を持つ。
カテゴリごとに1本まで作れて、`category is null` の行が既定ルール。

列 | 内容
:--|:--
category | 対象カテゴリ。null = 既定ルール
prefix / separator / digits | `AC` + `-` + 4桁 → `AC-0001`
next_number | 次に発番する番号
is_active | 使わないルールを残したまま止められる

商品の新規登録画面の「採番」で発番する。**押した時点で `next_number` を進めて番号を確保する**
（候補を見せるだけにすると、二人が同時に登録したとき同じ番号が出るため）。
登録をやめた分は欠番になる。既に使われているコードに当たった場合は、空いている番号まで進める
（旧シートから取り込んだコードとぶつかっても止まらないようにするため）。

## 7. CSV取込

旧シートの見出し（42列）をそのまま受け取る。並び順は問わない。

- 必須は「商品コード」「商品名」。自動計算列の見出しがあっても読み飛ばす
- 商品コードが一致すれば更新、無ければ新規
- **CSVに載っていない列は現在値のまま**。載っていた列だけを更新する
  （「商品コード＋期間出庫数」だけの販売データを毎日流しても、単価やリードタイムが 0 に戻らない）
- タブ区切り（スプレッドシートからのコピー&ペースト）もそのまま読める
- 取り込む前に「新規／更新／エラー／要確認」の件数を確認する画面を挟む

### 入出庫CSV

見出しは「日付 / 区分 / 商品コード / 数量 / 出荷先 / 伝票番号 / 備考」。必須は先頭4つ。

- 区分は 入庫 / 出庫 / 調整（`in` / `out` / `adjust`、入荷・出荷・払出などの表記ゆれも受ける）
- 出荷先は出庫の行、入庫元は入庫の行にだけ書ける（区分と相手先が食い違う行はエラーにする）
- 商品コードがマスタに無い行があると、**1件も取り込まない**。在庫を動かす台帳なので、
  半分だけ入った状態は後から追えなくなるため
- マスタに無い出荷先は既定ではエラー。「無い出荷先を自動で追加する」を選んだときだけ、
  名前だけの出荷先を作って取り込む

## 8. データの置き場所

既定は **ローカルDB**（PGlite ＝ PostgreSQL 17 をWASMにしたもの）。`webapp/.pglite/` に実体があり、
外部サービスの契約もDockerも要らない。`npm run dev` の初回起動でテーブルまで用意される。

`DATABASE_URL` を設定すると、そのPostgres（Supabase・自前サーバーなど）に切り替わる。
判定ビューを含めSQLは同じものが動くので、移行時に書き直す箇所は無い。

```
              画面（Server Component / Server Action）
                          │  生SQL（webapp/src/lib/db.ts）
        ┌─────────────────┴─────────────────┐
   ローカルDB（既定）                外部Postgres（DATABASE_URL）
   webapp/.pglite/                   Supabase など
```

バックアップは `webapp/.pglite/` をフォルダごとコピーするか、画面からCSVで書き出す。

## 9. 認証

無し。DBアクセスは全てサーバー側で行い、ブラウザからDBへは一切つながらない
（テーブルは RLS 有効・ポリシー無しなので、外部Postgresに載せ替えたときも
 anon キーからは直接読めない）。
社外から触れる場所へ公開するときは、置き場所側の制限（Basic認証・IP制限・社内ネットワーク限定）で
入口を塞ぐこと。
